def find_src_ip(packet):
    """Find the source IP address of the packet.

    Args:
        packet (pyshark.packet.packet.Packet): The packet to find the source IP address of.

    Returns:
        String: The source IP address of the packet.
    """
    ...
